{
  "name": "commandcode-proxy",
  "version": "2.0.0",
  "description": "Local-first credential vault and OpenAI-compatible proxy for Command Code and other AI providers",
  "main": "proxy.js",
  "scripts": {
    "start": "bun run proxy.js",
    "dev": "bun run --watch proxy.js",
    "test": "node test-proxy.js"
  },
  "keywords": ["command-code", "proxy", "openai", "api"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.4.2",
    "node-fetch": "^3.3.2"
  }
}
